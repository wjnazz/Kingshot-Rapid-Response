/* src/App.css */
:root {
  --primary-bg: #1a1a1a;
  --panel-bg: #2d2d2d;
  --text-main: #f0f0f0;
  --text-muted: #aaaaaa;
  --accent: #d4af37; /* Gold accent for fantasy feel */
  --accent-hover: #f1c40f;
  --border-color: #444;
}

body {
  margin: 0;
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background-color: var(--primary-bg);
  color: var(--text-main);
}

.app-container {
  max-width: 1000px;
  margin: 0 auto;
  padding: 20px;
}

header {
  text-align: center;
  margin-bottom: 40px;
  border-bottom: 2px solid var(--accent);
  padding-bottom: 20px;
}

header h1 {
  color: var(--accent);
  margin: 0 0 10px 0;
  font-size: 2.5em;
  text-transform: uppercase;
  letter-spacing: 2px;
}

header p {
  color: var(--text-muted);
  margin: 0;
  font-size: 1.2em;
}

.main-content {
  display: flex;
  gap: 30px;
}

@media (max-width: 768px) {
  .main-content {
    flex-direction: column;
  }
}

.form-container, .results-panel {
  flex: 1;
  background-color: var(--panel-bg);
  padding: 25px;
  border-radius: 8px;
  box-shadow: 0 4px 6px rgba(0,0,0,0.3);
  border: 1px solid var(--border-color);
}

h2 {
  margin-top: 0;
  border-bottom: 1px solid var(--border-color);
  padding-bottom: 10px;
  color: var(--accent);
}

.form-group {
  margin-bottom: 20px;
  display: flex;
  flex-direction: column;
}

label {
  margin-bottom: 8px;
  font-weight: bold;
}

select {
  padding: 10px;
  background-color: var(--primary-bg);
  color: var(--text-main);
  border: 1px solid var(--border-color);
  border-radius: 4px;
  font-size: 1rem;
}

select[multiple] {
  padding: 5px;
}

select[multiple] option {
  padding: 8px;
  margin-bottom: 2px;
  border-radius: 3px;
}

select[multiple] option:checked {
  background-color: var(--accent);
  color: #000;
}

small {
  color: var(--text-muted);
  margin-top: 5px;
  font-size: 0.8em;
}

/* Results Component Styles */
.results-placeholder {
  text-align: center;
  color: var(--text-muted);
  padding: 40px 0;
}

.result-section {
  margin-bottom: 20px;
  background-color: var(--primary-bg);
  padding: 15px;
  border-radius: 5px;
  border-left: 4px solid var(--accent);
}

.result-section h3 {
  margin: 0 0 10px 0;
  font-size: 1em;
  color: var(--text-muted);
  text-transform: uppercase;
}

.result-section p {
  margin: 0;
  font-size: 1.2em;
  font-weight: 500;
}

.highlight {
  color: var(--accent);
  font-weight: bold;
}
